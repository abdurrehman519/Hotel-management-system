PORT=5000
NODE_ENV=development

# Database options: set USE_SQLITE=true for local file-based database, or set to false to use MySQL
USE_SQLITE=true

DB_HOST=localhost
DB_PORT=3306
DB_NAME=hotel_management
DB_USER=root
DB_PASS=

JWT_SECRET=grand_horizon_hms_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d
